@page
@model OnlineExamination.Pages.QuestionsBank.DeleteModel
@{
}

<h2>Delete</h2>

    <h3>Are you sure you want to delete this Question?</h3>
    <div>
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Question.QuestionId)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Question.QuestionId)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Question.question)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Question.question)
            </dd>
        </dl>

        <form method="post">
            <button type="submit" class="btn btn-danger">Yes</button>
            <a asp-page="/QuestionsBank/List" class="btn btn-outline-info">Cancel</a>
            @*<a asp-page="/QuestionsBank/List" class="btn btn-outline-info">Back to List</a>*@
        </form>
    </div>